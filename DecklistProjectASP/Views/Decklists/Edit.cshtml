@model DecklistProjectASP.ViewModel.DeckEdit

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<h4>Decklist</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div class="text-danger"></div>
            <input type="hidden" asp-for="DecklistId" />
            <div class="form-group">
                <label asp-for="DeckName" class="control-label"></label>
                <input asp-for="DeckName" class="form-control" />
                <span asp-validation-for="DeckName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <div class="checkbox">
                    <label>
                        <input asp-for="isPublic" /> @Html.DisplayNameFor(model => model.isPublic)
                    </label>
                </div>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
